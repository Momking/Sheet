import React from "react";
import BackButton from "../Components/backButton";

const ConfigureSettings = () => {
  return (
    <div>
      <BackButton destination={"/DoctorsSheet"} />
    </div>
  );
};

export default ConfigureSettings;
